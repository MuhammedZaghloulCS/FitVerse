@{
    ViewData["Title"] = "Add New Coach";
    Layout = "~/Views/Shared/_CoachLayout.cshtml";
}
<!-- ===========================
<!-- Main Content -->
<div class="container-fluid px-4 py-4">
    <!-- Page Header -->
    <h2 class="fw-bold mb-1">Welcome back, Coach 👋</h2>
    <p class="text-muted mb-4">Here's an overview of your coaching activities</p>

    <div class="row g-4 mb-4">
        <div class="col-xl-3 col-md-6">
            <div class="stat-card primary">
                <div class="stat-icon primary"><i class="bi bi-people"></i></div>
                <div class="stat-value">@Model.ActiveClientsCount</div>
                <div class="stat-label">Active Clients</div>
            </div>
        </div>
        <div class="col-xl-3 col-md-6">
            <div class="stat-card success">
                <div class="stat-icon success"><i class="bi bi-calendar-check"></i></div>
                <div class="stat-value">@Model.TotalPlans</div>
                <div class="stat-label">Workout Plans</div>
            </div>
        </div>
        <div class="col-xl-3 col-md-6">
            <div class="stat-card warning">
                <div class="stat-icon warning"><i class="bi bi-lightning"></i></div>
                <div class="stat-value">@Model.TotalExercises</div>
                <div class="stat-label">Total Exercises</div>
            </div>
        </div>
        <div class="col-xl-3 col-md-6">
            <div class="stat-card danger">
                <div class="stat-icon danger"><i class="bi bi-star"></i></div>
                <div class="stat-value">@Model.AverageRating.ToString("0.0")</div>
                <div class="stat-label">Average Rating</div>
            </div>
        </div>
    </div>
    <
</div>
<!-- Main Content Grid -->
<div class="row g-4 mb-4">
    <!-- Client Activity Chart -->
    <div class="col-lg-8">
        <div class="card-custom">
            <div class="card-header-custom">
                <i class="bi bi-graph-up me-2"></i> Client Activity Overview
            </div>
            <div class="card-body-custom">
                <canvas id="activityChart" height="80"></canvas>
            </div>
        </div>
    </div>

    <!-- Quick Actions -->
    <div class="col-lg-4">
        <div class="card-custom">
            <div class="card-header-custom">
                <i class="bi bi-lightning-charge me-2"></i> Quick Actions
            </div>
            <div class="card-body-custom">
                <div class="d-grid gap-3">
                    <button class="btn btn-primary-custom" data-bs-toggle="modal" data-bs-target="#createPlanModal">
                        <i class="bi bi-plus-circle me-2"></i> Create Workout Plan
                    </button>
                    <button class="btn btn-success-custom" data-bs-toggle="modal" data-bs-target="#addExerciseModal">
                        <i class="bi bi-lightning me-2"></i> Add Exercise
                    </button>
                    <button class="btn btn-outline-custom" id="viewAllClientsBtn">
                        <i class="bi bi-people me-2"></i> View All Clients
                    </button>
                    <button class="btn btn-outline-custom">
                        <i class="bi bi-chat-dots me-2"></i> Open Messages
                    </button>
                </div>

                <div class="divider"></div>

                <div class="text-center">
                    <h6 class="fw-bold mb-3">Your Performance</h6>
                    <canvas id="performanceChart"></canvas>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Recent Clients & Pending Tasks -->
<div class="col-lg-6">
    <div class="card-custom">
        <div class="card-header-custom d-flex justify-content-between align-items-center">
            <span><i class="bi bi-people me-2"></i> Recent Clients</span>
            <a href="coach/index.html" class="text-white text-decoration-none small">View All</a>
        </div>
        <div class="card-body-custom">
            @if (Model.RecentClients != null && Model.RecentClients.Any())
            {
                foreach (var client in Model.RecentClients)
                {
                    <div class="d-flex align-items-center justify-content-between mb-3 pb-3 border-bottom">
                        <div class="d-flex align-items-center gap-3">
                            <div>

                                <div class="fw-semibold">@client.Name</div>
                                <div class="text-muted small">Last payment: @client.LastPaymentAgo</div>
                            </div>
                        </div>
                        <div class="text-end">
                            <span class="badge-custom @(client.IsActive ? "badge-success" : "badge-warning")">
                                @(client.IsActive ? "Active" : "Inactive")
                            </span>

                        </div>
                    </div>
                }
            }
            else
            {
                <div class="text-center text-muted">No clients found.</div>
            }
        </div>
    </div>
</div>


<!-- Create Plan Modal -->
<div class="modal fade" id="createPlanModal" tabindex="-1">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content" style="border-radius: 16px; border: none;">
            <div class="modal-header" style="background: linear-gradient(135deg, #6366f1 0%, #4f46e5 100%); color: white; border: none;">
                <h5 class="modal-title fw-bold">Create Workout Plan</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body p-4">
                <form>
                    <div class="mb-3">
                        <label class="form-label-custom">Plan Name</label>
                        <input type="text" class="form-control-custom" placeholder="e.g., Full Body Strength Training" required>
                    </div>

                    <div class="row g-3 mb-3">
                        <div class="col-md-6">
                            <label class="form-label-custom">Assign to Client</label>
                            <select class="form-control-custom" required>
                                <option value="">Select Client</option>
                                <option>Sarah Miller</option>
                                <option>John Smith</option>
                                <option>Emma Davis</option>
                            </select>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label-custom">Duration (weeks)</label>
                            <input type="number" class="form-control-custom" placeholder="8" required>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label-custom">Description</label>
                        <textarea class="form-control-custom" rows="3" placeholder="Describe the workout plan goals and approach..."></textarea>
                    </div>

                    <div class="mb-3">
                        <label class="form-label-custom">Difficulty Level</label>
                        <select class="form-control-custom" required>
                            <option>Beginner</option>
                            <option>Intermediate</option>
                            <option>Advanced</option>
                        </select>
                    </div>
                </form>
            </div>
            <div class="modal-footer border-0">
                <button type="button" class="btn btn-outline-custom" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-primary-custom">Create Plan</button>
            </div>
        </div>
    </div>
</div>

<!-- Add Exercise Modal -->
<!-- Add Exercise Modal -->
<div class="modal fade" id="addExerciseModal" tabindex="-1">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content" style="border-radius: 16px; border: none;">
            <div class="modal-header" style="background: linear-gradient(135deg, #10b981 0%, #059669 100%); color: white; border: none;">
                <h5 class="modal-title fw-bold">Add New Exercise</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body p-4">
                <form id="exerciseForm">
                    <div class="mb-3">
                        <label class="form-label-custom">Exercise Name</label>
                        <input type="text" id="Name" name="Name" class="form-control-custom" placeholder="e.g., Barbell Bench Press" required>
                    </div>

                    <div class="row g-3 mb-3">
                        <div class="col-md-6">
                            <label class="form-label-custom">Muscle Group</label>
                            <select name="muscleId" id="muscleId" class="form-control-custom" required>
                                <option value="">Select Muscle </option>
                            </select>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label-custom">Equipment Group</label>
                        <select name="equipmentId" id="equipmentId" class="form-control-custom" required>
                            <option value="">Select Equipment </option>
                        </select>
                    </div>

                    <div class="mb-3">
                        <label class="form-label-custom">Description</label>
                        <textarea id="description" name="Description" class="form-control-custom" rows="4" placeholder="Detailed instructions..."></textarea>
                    </div>

                    <div class="mb-3">
                        <label class="form-label-custom">Video URL (Optional)</label>
                        <input type="url" id="videoLink" name="VideoUrl" class="form-control-custom" placeholder="https://youtube.com/...">
                    </div>
                    <div class="modal-footer border-0">
                        <button type="button" class="btn btn-outline-custom" data-bs-dismiss="modal">Cancel</button>
                        <button type="submit" class="btn btn-success-custom" id="saveExerciseBtn">Add Exercise</button>
                    </div>
                </form>
            </div>

        </div>
    </div>
</div>
<!-- View All Clients Modal -->
<div class="modal fade" id="viewAllClientsModal" tabindex="-1">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content" style="border-radius: 16px; border: none;">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title fw-bold">All Clients</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body p-4">
                <div id="allClientsContainer">
                    <!-- هنا هيتحط كل العملاء بالAjax -->
                    <div class="text-center text-muted">Loading...</div>
                </div>
            </div>
            <div class="modal-footer border-0">
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>


@* <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script> *@
@* <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script> *@
@section Scripts{
    <script src="/ViewJs/exercise.js"></script>
}
